# ==========================================
# 前端环境变量配置示例
# ==========================================
# 构建生产环境前，请复制此文件为 .env 并修改下方变量
# 开发环境无需修改，默认使用 localhost

# API 后端域名（用于 Scratch 作品 iframe 加载项目文件）
# 开发环境默认值: http://localhost:5001
# 生产环境必须改为实际域名，例如: https://kidsshowcode.cn
# 注意: 不要带末尾斜杠，不要带 /api 路径
VITE_API_ORIGIN=http://localhost:5001
